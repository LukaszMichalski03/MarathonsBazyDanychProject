@using BDProject_MarathonesApp.Models
@model List<User>

@{
    ViewData["Title"] = "ClubMembers";
}

<nav class="navbar navbar-expand-lg navbar-light bg-light p-3">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarNavDropdown">
        <ul class="navbar-nav">

            <li class="nav-item">
                <a class="nav-link" asp-controller="Admin" asp-action="AllRaces">Biegi</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-controller="Admin" asp-action="AllUsers">Użytkownicy</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-controller="Admin" asp-action="AllClubs">Kluby</a>
            </li>
        </ul>
        <ul class="navbar-nav">

            <li class="nav-item">
                <a class="nav-link" asp-controller="Account" asp-action="Login">Wyloguj</a>
            </li>
        </ul>
    </div>
</nav>


<main role="main">

    <div class="list-group p-5">
        @foreach (var user in Model)
        {
            <div class="list-group-item flex-column align-items-start m-2">
                <div class="d-flex w-100 ">
                    <h5 class="mb-1">@user.Name @user.LastName</h5>



                </div>
                <div class="d-flex w-100 ">
                    <p class="mb-1">@user.Login</p>
                    @if (user.Address != null)
                    {
                        <p class="mb-1">@user.Address.City</p>
                    }
                </div>
                

                <div class="d-flex gap-4 ms-1">
                    <form method="post" asp-controller="Admin" asp-action="DeleteClubMember" asp-route-userId="@user.Id">
                        <button type="submit" class="btn btn-danger">Usun z klubu</button>
                    </form>

                </div>

            </div>

        }
    </div>

</main>
